name: Legacy research cache disabled

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: read

jobs:
  forbidden:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Fail closed
        shell: bash
        run: |
          echo "The legacy research cache is forbidden and this workflow may not create it."
          exit 1
