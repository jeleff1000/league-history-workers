name: Weekly Update Worker

# Updates all fantasy football leagues with latest data.
# Triggered by update_nfl_super_table.yml after NFL data is refreshed.
# Can also be triggered manually for a specific league.
# NOTE: NFL super table must be updated BEFORE running this workflow.

on:
  workflow_dispatch:
    inputs:
      database_name:
        description: 'MotherDuck database name (leave empty for all leagues)'
        required: false
        type: string
      week:
        description: 'Force specific week (leave empty for auto-detect)'
        required: false
        type: string
      dry_run:
        description: 'Dry run (show what would happen without doing it)'
        required: false
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  # First, discover all leagues and detect current NFL week (ONE API call for all)
  discover-leagues:
    runs-on: ubuntu-latest
    outputs:
      leagues: ${{ steps.list.outputs.leagues }}
      league_count: ${{ steps.list.outputs.count }}
      current_week: ${{ steps.week.outputs.current_week }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          repository: jeleff1000/yahoo_oauth
          token: ${{ secrets.PRIVATE_REPO_PAT }}

      - name: Setup Python for discovery
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install duckdb yahoo_oauth requests

      - name: List registered leagues
        id: list
        env:
          MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
          TARGET_DB: ${{ github.event.inputs.database_name || '' }}
        run: |
          # If specific database requested, just use that
          if [ -n "$TARGET_DB" ]; then
            echo "leagues=[\"${TARGET_DB}\"]" >> $GITHUB_OUTPUT
            echo "count=1" >> $GITHUB_OUTPUT
            echo "Targeting specific database: ${TARGET_DB}"
            exit 0
          fi

          # Query MotherDuck to discover all fantasy football databases
          python - <<'PYEOF'
          import duckdb
          import os
          import json

          token = os.environ.get('MOTHERDUCK_TOKEN', '')
          if not token:
              print("No MOTHERDUCK_TOKEN found")
              with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
                  f.write('leagues=[]\n')
                  f.write('count=0\n')
              exit(0)

          try:
              conn = duckdb.connect(f"md:?motherduck_token={token}")

              # Get all league databases from registry (fast — avoids scanning 500+ attached DBs)
              registry_rows = conn.execute("""
                  SELECT DISTINCT database_name FROM (
                      SELECT database_name FROM ___ops.main.league_credentials
                      UNION ALL
                      SELECT database_name FROM ___ops.main.sleeper_leagues
                      UNION ALL
                      SELECT database_name FROM ___ops.main.espn_leagues
                  )
                  ORDER BY database_name
              """).fetchall()
              fantasy_dbs = [row[0] for row in registry_rows if row[0]]
              print(f"  Found {len(fantasy_dbs)} league databases from registry")

              conn.close()

              print(f"\nFound {len(fantasy_dbs)} fantasy football databases")

              with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
                  f.write(f'leagues={json.dumps(fantasy_dbs)}\n')
                  f.write(f'count={len(fantasy_dbs)}\n')

          except Exception as e:
              print(f"Error discovering databases: {e}")
              with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
                  f.write('leagues=[]\n')
                  f.write('count=0\n')
          PYEOF

      - name: Detect current NFL week (once for all leagues)
        id: week
        env:
          YAHOO_CLIENT_ID: ${{ secrets.YAHOO_CLIENT_ID }}
          YAHOO_CLIENT_SECRET: ${{ secrets.YAHOO_CLIENT_SECRET }}
          MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
          CREDENTIAL_ENCRYPTION_KEY: ${{ secrets.CREDENTIAL_ENCRYPTION_KEY }}
          MANUAL_WEEK: ${{ github.event.inputs.week || '' }}
        run: |
          # If week manually specified, use that
          if [ -n "$MANUAL_WEEK" ]; then
            echo "current_week=${MANUAL_WEEK}" >> $GITHUB_OUTPUT
            echo "Using manually specified week: ${MANUAL_WEEK}"
            exit 0
          fi

          python - <<'PYEOF'
          import os
          import sys
          import json
          import time
          from datetime import date, timedelta
          from pathlib import Path

          def date_based_week_estimate():
              """Fallback: estimate NFL week from date (Week 1 ~= Thursday after Labor Day)"""
              today = date.today()
              year = today.year

              # Find Labor Day (first Monday of September)
              sept1 = date(year, 9, 1)
              days_to_monday = (7 - sept1.weekday()) % 7
              if sept1.weekday() == 0:
                  labor_day = sept1
              else:
                  labor_day = sept1 + timedelta(days=days_to_monday)

              # Week 1 starts Thursday after Labor Day
              week1_start = labor_day + timedelta(days=3)

              if today < week1_start:
                  return 1  # Preseason

              days_since_week1 = (today - week1_start).days
              current_week = (days_since_week1 // 7) + 1
              return min(max(current_week, 1), 18)  # Clamp to 1-18

          def get_week_from_yahoo():
              """Get current week from Yahoo API using stored credentials"""
              try:
                  # Try to get credentials from any league
                  sys.path.insert(0, 'fantasy_football_data_scripts')
                  from multi_league.utils.credential_store import retrieve_league_credentials
                  import duckdb

                  token = os.environ.get('MOTHERDUCK_TOKEN', '')
                  if not token:
                      return None

                  # Find first league with credentials (query registry directly)
                  conn = duckdb.connect(f"md:?motherduck_token={token}")
                  refresh_token = None
                  league_key = None

                  cred_rows = conn.execute("""
                      SELECT database_name, league_id, encrypted_refresh_token
                      FROM ___ops.main.league_credentials
                      WHERE encrypted_refresh_token IS NOT NULL
                      LIMIT 1
                  """).fetchall()
                  conn.close()

                  for db_name, lid, enc_token in cred_rows:
                      try:
                          creds = retrieve_league_credentials(db_name)
                          if creds and creds.get('refresh_token'):
                              refresh_token = creds['refresh_token']
                              league_key = lid
                              print(f"Using credentials from {db_name}, league_key: {league_key}")
                              break
                      except:
                          continue

                  if not refresh_token:
                      print("No stored credentials found")
                      return None

                  # Create OAuth file and get current week
                  consumer_key = os.environ.get('YAHOO_CLIENT_ID')
                  consumer_secret = os.environ.get('YAHOO_CLIENT_SECRET')

                  if not consumer_key or not consumer_secret:
                      print("Missing Yahoo credentials")
                      return None

                  # Create temp OAuth file
                  oauth_data = {
                      'consumer_key': consumer_key,
                      'consumer_secret': consumer_secret,
                      'refresh_token': refresh_token,
                      'token_time': time.time() - 7200,
                  }

                  Path("oauth").mkdir(exist_ok=True)
                  with open("oauth/Oauth.json", 'w') as f:
                      json.dump(oauth_data, f)

                  # Query Yahoo for current week
                  from yahoo_oauth import OAuth2
                  oauth = OAuth2(None, None, from_file="oauth/Oauth.json")

                  if not oauth.token_is_valid():
                      oauth.refresh_access_token()

                  # Get current NFL game info
                  year = date.today().year
                  url = f"https://fantasysports.yahooapis.com/fantasy/v2/game/nfl"
                  resp = oauth.session.get(url)

                  if resp.status_code == 200:
                      import re
                      # Parse current_week from XML response
                      match = re.search(r'<current_week>(\d+)</current_week>', resp.text)
                      if match:
                          week = int(match.group(1))
                          print(f"Yahoo API returned current_week: {week}")
                          return week

                  print(f"Yahoo API call failed: {resp.status_code}")
                  return None

              except Exception as e:
                  print(f"Error getting week from Yahoo: {e}")
                  return None

          # Try Yahoo first, fall back to date-based estimate
          current_week = get_week_from_yahoo()

          if current_week is None:
              current_week = date_based_week_estimate()
              print(f"Using date-based estimate: week {current_week}")

          # Determine target week based on day of week
          # On Tuesday-Saturday: the current_week just finished (MNF is Monday night)
          # On Sunday-Monday: games still in progress, so previous week is complete
          from datetime import date
          today = date.today()
          weekday = today.weekday()  # 0=Monday, 1=Tuesday, ..., 6=Sunday

          if weekday >= 1 and weekday <= 5:  # Tuesday-Saturday
              # Week's games are complete (MNF ended Monday night)
              target_week = current_week
          else:  # Sunday or Monday
              # Games still in progress
              target_week = max(1, current_week - 1)

          print(f"Current NFL week: {current_week}, today is weekday {weekday}, targeting completed week: {target_week}")

          with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
              f.write(f'current_week={target_week}\n')
          PYEOF

  # Update each league (pulls from shared NFL super table, already updated by update_nfl_super_table.yml)
  update-league:
    needs: discover-leagues
    if: needs.discover-leagues.outputs.league_count != '0'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    concurrency:
      group: central-db-write-${{ matrix.database }}
      cancel-in-progress: false

    strategy:
      fail-fast: false  # Continue updating other leagues even if one fails
      matrix:
        database: ${{ fromJson(needs.discover-leagues.outputs.leagues) }}

    env:
      PYTHONUNBUFFERED: 1

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          repository: jeleff1000/yahoo_oauth
          token: ${{ secrets.PRIVATE_REPO_PAT }}

      - name: Setup Python
        id: setup-python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Cache Python virtualenv
        uses: actions/cache@v5
        id: cache-venv
        with:
          path: .venv
          key: venv-${{ runner.os }}-py${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('requirements.txt') }}

      - name: Create virtualenv
        if: steps.cache-venv.outputs.cache-hit != 'true'
        run: python -m venv .venv

      - name: Install dependencies
        run: |
          source .venv/bin/activate
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Activate virtualenv
        run: echo "${{ github.workspace }}/.venv/bin" >> $GITHUB_PATH

      - name: Download existing data from MotherDuck
        id: download
        env:
          MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
          DATABASE_NAME: ${{ matrix.database }}
        run: |
          echo "========================================"
          echo "Downloading existing data for: ${DATABASE_NAME}"
          echo "========================================"

          mkdir -p fantasy_football_data

          python - <<'PYEOF'
          import duckdb
          import os
          from pathlib import Path

          db_name = os.environ['DATABASE_NAME']
          token = os.environ['MOTHERDUCK_TOKEN']
          output_dir = Path("fantasy_football_data")

          print(f"Connecting to MotherDuck database: {db_name}")

          try:
              conn = duckdb.connect(f"md:{db_name}?motherduck_token={token}")

              # Get list of tables
              tables = conn.execute("SHOW TABLES").fetchall()
              print(f"Found {len(tables)} tables")

              for (table_name,) in tables:
                  print(f"  Downloading {table_name}...")
                  output_file = output_dir / f"{table_name}.parquet"
                  conn.execute(f"COPY (SELECT * FROM {table_name}) TO '{output_file}' (FORMAT PARQUET)")
                  print(f"    -> {output_file}")

              conn.close()
              print("✅ Download complete")

          except Exception as e:
              print(f"❌ Error downloading data: {e}")
              # Continue anyway - may be a new database
              exit(0)
          PYEOF

      - name: Reconstruct league context
        id: context
        env:
          DATABASE_NAME: ${{ matrix.database }}
        run: |
          # Reconstruct context from league_context.parquet (stored by initial import)
          # Falls back to league_settings.parquet for backwards compatibility
          python - <<'PYEOF'
          import json
          import os
          from pathlib import Path
          import pandas as pd

          db_name = os.environ['DATABASE_NAME']
          data_dir = Path("fantasy_football_data")

          # Format league name for display (strip l_ prefix if present for digit-starting names)
          display_name = db_name
          if db_name.startswith('l_') and len(db_name) > 2 and db_name[2].isdigit():
              display_name = db_name[2:]  # Strip the l_ prefix
          league_name = display_name.replace('_', ' ').title()

          # PREFERRED: Try to read from league_context.parquet (stored by initial import)
          context_file = data_dir / "league_context.parquet"
          if context_file.exists():
              print(f"✅ Found league_context.parquet - using stored context")
              df = pd.read_parquet(context_file)
              if not df.empty:
                  row = df.iloc[0]

                  context = {
                      "league_id": row.get('league_id', db_name),
                      "league_name": row.get('league_name', league_name),
                      "oauth_file_path": None,  # Will use embedded oauth_credentials
                      "oauth_credentials": None,  # Populated in next step
                      "game_code": row.get('game_code', 'nfl'),
                      "start_year": int(row.get('start_year', 2014)),
                      "end_year": int(row.get('end_year', 2024)),
                      "num_teams": int(row.get('num_teams')) if pd.notna(row.get('num_teams')) else None,
                      "playoff_teams": int(row.get('playoff_teams')) if pd.notna(row.get('playoff_teams')) else None,
                      "regular_season_weeks": int(row.get('regular_season_weeks')) if pd.notna(row.get('regular_season_weeks')) else None,
                      "data_directory": str(data_dir.resolve()),
                      "max_workers": 5,
                      "enable_caching": True,
                      "rate_limit_per_sec": 4.0,
                      "motherduck_db_name": db_name,
                  }

                  # Parse JSON columns
                  try:
                      league_ids_json = row.get('league_ids_json', '{}')
                      if league_ids_json and pd.notna(league_ids_json):
                          context['league_ids'] = json.loads(league_ids_json)
                          print(f"   Loaded {len(context['league_ids'])} league IDs")
                  except Exception as e:
                      print(f"   ⚠️ Could not parse league_ids_json: {e}")

                  try:
                      keeper_rules_json = row.get('keeper_rules_json')
                      if keeper_rules_json and pd.notna(keeper_rules_json):
                          context['keeper_rules'] = json.loads(keeper_rules_json)
                  except:
                      pass

                  try:
                      overrides_json = row.get('manager_name_overrides_json', '{}')
                      if overrides_json and pd.notna(overrides_json):
                          context['manager_name_overrides'] = json.loads(overrides_json)
                  except:
                      pass

                  with open("league_context.json", 'w') as f:
                      json.dump(context, f, indent=2)

                  print(f"✅ Reconstructed context for {db_name}")
                  print(f"   Years: {context.get('start_year')} - {context.get('end_year')}")
                  if context.get('league_ids'):
                      print(f"   League IDs: {list(context['league_ids'].keys())}")
                  exit(0)

          # FALLBACK: Try to read league info from league_settings.parquet (older imports)
          print(f"⚠️ No league_context.parquet found - falling back to league_settings.parquet")
          settings_file = data_dir / "league_settings.parquet"

          if settings_file.exists():
              df = pd.read_parquet(settings_file)
              if not df.empty:
                  # Get most recent year's settings
                  latest = df.sort_values('year', ascending=False).iloc[0]

                  context = {
                      "league_id": latest.get('league_key', db_name),
                      "league_name": league_name,
                      "oauth_file_path": None,
                      "oauth_credentials": None,
                      "game_code": "nfl",
                      "start_year": int(df['year'].min()),
                      "end_year": int(df['year'].max()),
                      "num_teams": int(latest.get('num_teams', 10)),
                      "playoff_teams": int(latest.get('num_playoff_teams', 6)),
                      "data_directory": str(data_dir.resolve()),
                      "max_workers": 5,
                      "enable_caching": True,
                      "rate_limit_per_sec": 4.0,
                  }

                  # Try to load league_ids mapping from settings_json
                  try:
                      settings_json = json.loads(latest.get('settings_json', '{}'))
                      if 'league_ids' in settings_json:
                          context['league_ids'] = settings_json['league_ids']
                  except:
                      pass

                  with open("league_context.json", 'w') as f:
                      json.dump(context, f, indent=2)

                  print(f"✅ Reconstructed context from league_settings for {db_name}")
                  print(json.dumps(context, indent=2))
                  exit(0)

          # LAST RESORT: create minimal context but try to infer years from matchup data
          print(f"⚠️ No settings files found - creating context from matchup data")

          # Try to get year range from matchup table (should always exist)
          start_year = 2020
          end_year = 2024
          league_ids = {}

          matchup_file = data_dir / "matchup.parquet"
          if matchup_file.exists():
              try:
                  mdf = pd.read_parquet(matchup_file)
                  if 'year' in mdf.columns:
                      start_year = int(mdf['year'].min())
                      end_year = int(mdf['year'].max())
                      print(f"   Inferred years from matchup data: {start_year} - {end_year}")

                      # Try to get league_key per year for league_ids mapping
                      if 'league_key' in mdf.columns:
                          for year in range(start_year, end_year + 1):
                              year_data = mdf[mdf['year'] == year]
                              if not year_data.empty and 'league_key' in year_data.columns:
                                  lk = year_data['league_key'].iloc[0]
                                  if pd.notna(lk):
                                      league_ids[str(year)] = lk
                          if league_ids:
                              print(f"   Inferred {len(league_ids)} league IDs from matchup data")
              except Exception as e:
                  print(f"   Could not read matchup data: {e}")

          context = {
              "league_id": db_name,
              "league_name": league_name,
              "oauth_file_path": None,
              "oauth_credentials": None,
              "game_code": "nfl",
              "start_year": start_year,
              "end_year": end_year,
              "data_directory": str(data_dir.resolve()),
              "max_workers": 5,
              "enable_caching": True,
              "rate_limit_per_sec": 4.0,
          }

          if league_ids:
              context['league_ids'] = league_ids

          with open("league_context.json", 'w') as f:
              json.dump(context, f, indent=2)

          # Also save as parquet so it persists to MotherDuck after this run
          from datetime import datetime
          now = datetime.now().isoformat()
          row = {
              'league_id': context['league_id'],
              'league_name': context['league_name'],
              'game_code': context.get('game_code', 'nfl'),
              'start_year': context.get('start_year'),
              'end_year': context.get('end_year'),
              'num_teams': context.get('num_teams'),
              'playoff_teams': context.get('playoff_teams'),
              'regular_season_weeks': context.get('regular_season_weeks'),
              'league_ids_json': json.dumps(league_ids) if league_ids else '{}',
              'keeper_rules_json': json.dumps(context.get('keeper_rules')) if context.get('keeper_rules') else None,
              'manager_name_overrides_json': json.dumps(context.get('manager_name_overrides', {})),
              'created_at': context.get('created_at') or now,
              'updated_at': now,
          }
          ctx_df = pd.DataFrame([row])
          ctx_df.to_parquet(data_dir / "league_context.parquet", index=False)
          print(f"   ✅ Created league_context.parquet (will be saved to MotherDuck)")

          print(json.dumps(context, indent=2))
          PYEOF

      - name: Retrieve and embed credentials
        id: creds
        env:
          MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
          CREDENTIAL_ENCRYPTION_KEY: ${{ secrets.CREDENTIAL_ENCRYPTION_KEY }}
          YAHOO_CLIENT_ID: ${{ secrets.YAHOO_CLIENT_ID }}
          YAHOO_CLIENT_SECRET: ${{ secrets.YAHOO_CLIENT_SECRET }}
          YAHOO_MASTER_REFRESH_TOKEN: ${{ secrets.YAHOO_MASTER_REFRESH_TOKEN }}
          DATABASE_NAME: ${{ matrix.database }}
        run: |
          echo "========================================"
          echo "Retrieving and embedding credentials for: ${DATABASE_NAME}"
          echo "========================================"

          python - <<'PYEOF'
          import json
          import os
          import sys
          import time
          from pathlib import Path

          sys.path.insert(0, 'fantasy_football_data_scripts')

          database_name = os.environ['DATABASE_NAME']
          consumer_key = os.environ.get('YAHOO_CLIENT_ID')
          consumer_secret = os.environ.get('YAHOO_CLIENT_SECRET')
          encryption_key = os.environ.get('CREDENTIAL_ENCRYPTION_KEY')
          master_refresh_token = os.environ.get('YAHOO_MASTER_REFRESH_TOKEN')

          refresh_token = None
          access_token = None
          token_source = None
          token_time = time.time() - 7200  # Default: force refresh

          # Try to retrieve stored credentials from MotherDuck
          if encryption_key:
              try:
                  from multi_league.utils.credential_store import retrieve_league_credentials
                  creds = retrieve_league_credentials(database_name)
                  if creds:
                      refresh_token = creds.get('refresh_token')
                      token_source = "per-league"
                      print(f"✅ Retrieved stored credentials for {creds.get('league_name')}")
              except Exception as e:
                  print(f"⚠️ Could not retrieve stored credentials: {e}")
          else:
              print("⚠️ CREDENTIAL_ENCRYPTION_KEY not set - cannot decrypt stored credentials")

          # Validate and refresh per-league token if we have one
          token_valid = False
          if refresh_token and consumer_key and consumer_secret:
              print("🔍 Validating per-league token...")
              try:
                  import tempfile
                  # Create temp OAuth file to test
                  # IMPORTANT: access_token must be non-null for yahoo_oauth to attempt refresh
                  test_oauth = {
                      'consumer_key': consumer_key,
                      'consumer_secret': consumer_secret,
                      'access_token': 'expired_placeholder',
                      'refresh_token': refresh_token,
                      'token_time': 0,  # Force expired so library refreshes
                  }
                  with tempfile.NamedTemporaryFile(mode='w', suffix='.json', delete=False) as f:
                      json.dump(test_oauth, f)
                      test_file = f.name

                  # Try to refresh the token
                  from yahoo_oauth import OAuth2
                  oauth = OAuth2(None, None, from_file=test_file)
                  if not oauth.token_is_valid():
                      oauth.refresh_access_token()

                  # Get the fresh tokens
                  access_token = getattr(oauth, 'access_token', None)
                  new_refresh = getattr(oauth, 'refresh_token', refresh_token)
                  token_time = time.time()
                  token_valid = True
                  print("✅ Per-league token refreshed successfully")

                  # Save the new refresh token back to MotherDuck (keeps it alive)
                  if new_refresh and new_refresh != refresh_token:
                      print("🔄 Saving refreshed token back to storage...")
                      try:
                          from multi_league.utils.credential_store import store_league_credentials
                          store_league_credentials(
                              database_name=database_name,
                              refresh_token=new_refresh,
                              league_id=creds.get('league_id', database_name),
                              league_name=creds.get('league_name', database_name)
                          )
                          refresh_token = new_refresh
                          print("✅ Token persisted - will stay valid for future runs")
                      except Exception as store_err:
                          print(f"⚠️ Could not persist token: {store_err}")

                  os.unlink(test_file)
              except Exception as e:
                  print(f"⚠️ Per-league token validation failed: {e}")
                  token_valid = False

          # Fallback to master token if per-league not available or invalid
          if (not refresh_token or not token_valid) and master_refresh_token:
              refresh_token = master_refresh_token
              token_source = "master"
              print("📌 Using master refresh token as fallback")

              # Refresh the master token to get access_token
              if consumer_key and consumer_secret:
                  try:
                      import tempfile
                      # IMPORTANT: access_token must be non-null for yahoo_oauth to attempt refresh
                      test_oauth = {
                          'consumer_key': consumer_key,
                          'consumer_secret': consumer_secret,
                          'access_token': 'expired_placeholder',
                          'refresh_token': refresh_token,
                          'token_time': 0,  # Force expired so library refreshes
                      }
                      with tempfile.NamedTemporaryFile(mode='w', suffix='.json', delete=False) as f:
                          json.dump(test_oauth, f)
                          test_file = f.name

                      from yahoo_oauth import OAuth2
                      oauth = OAuth2(None, None, from_file=test_file)
                      if not oauth.token_is_valid():
                          oauth.refresh_access_token()

                      access_token = getattr(oauth, 'access_token', None)
                      refresh_token = getattr(oauth, 'refresh_token', refresh_token)
                      token_time = time.time()
                      os.unlink(test_file)
                      print("✅ Master token refreshed successfully")
                  except Exception as e:
                      print(f"⚠️ Master token refresh failed: {e}")

          if not refresh_token:
              print("⚠️ No refresh token available - data fetching may fail")
              print("   This league may need to be re-imported to store credentials")
              print("   Or add YAHOO_MASTER_REFRESH_TOKEN as a GitHub secret for fallback")

          # Build oauth data
          # IMPORTANT: access_token must be a non-null string for yahoo_oauth to attempt refresh
          # If null, the library skips refresh and goes straight to interactive auth
          oauth_data = {
              'consumer_key': consumer_key,
              'consumer_secret': consumer_secret,
              'access_token': access_token or 'expired_placeholder',
              'refresh_token': refresh_token,
              'token_time': 0,  # Force token to appear expired so library refreshes it
              'token_type': 'bearer',
          }

          # Write oauth/Oauth.json for child scripts that still use file-based OAuth
          import os
          os.makedirs("oauth", exist_ok=True)
          with open("oauth/Oauth.json", 'w') as f:
              json.dump(oauth_data, f, indent=2)
          print("📁 Created oauth/Oauth.json for child script compatibility")

          # Read the existing league_context.json and embed credentials
          with open("league_context.json", 'r') as f:
              context = json.load(f)

          # Embed oauth_credentials directly (like local setup)
          context['oauth_credentials'] = oauth_data
          context['oauth_file_path'] = str(Path("oauth/Oauth.json").resolve())

          # Write updated context
          with open("league_context.json", 'w') as f:
              json.dump(context, f, indent=2)

          print("📋 Updated league_context.json with embedded oauth_credentials:")
          # Print context without sensitive tokens
          safe_context = dict(context)
          if safe_context.get('oauth_credentials'):
              safe_creds = dict(safe_context['oauth_credentials'])
              if safe_creds.get('access_token'):
                  safe_creds['access_token'] = safe_creds['access_token'][:20] + '...'
              if safe_creds.get('refresh_token'):
                  safe_creds['refresh_token'] = safe_creds['refresh_token'][:20] + '...'
              safe_context['oauth_credentials'] = safe_creds
          print(json.dumps(safe_context, indent=2))

          # Set output for later steps
          has_token = "true" if refresh_token else "false"
          with open(os.environ['GITHUB_OUTPUT'], 'a') as f:
              f.write(f"has_refresh_token={has_token}\n")
              f.write(f"token_source={token_source or 'none'}\n")

          if refresh_token:
              print(f"✅ OAuth credentials embedded (source: {token_source})")
          else:
              print("⚠️ Context created but may not work without refresh token")
          PYEOF

      - name: Run weekly update
        id: update
        env:
          DATABASE_NAME: ${{ matrix.database }}
          MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
          DETECTED_WEEK: ${{ needs.discover-leagues.outputs.current_week }}
          DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }}
        working-directory: fantasy_football_data_scripts
        run: |
          echo "========================================"
          echo "Running weekly update for: ${DATABASE_NAME}"
          echo "Using pre-detected week: ${DETECTED_WEEK}"
          echo "========================================"

          ARGS="--context ../league_context.json"

          # Use the week detected once in discover-leagues (no per-league API call)
          if [ -n "$DETECTED_WEEK" ]; then
            ARGS="$ARGS --week $DETECTED_WEEK"
          fi

          if [ "$DRY_RUN" == "true" ]; then
            ARGS="$ARGS --dry-run"
          fi

          echo "Command: python weekly_update_v2.py $ARGS"

          python weekly_update_v2.py $ARGS 2>&1 | tee ../weekly_update.log

          EXIT_CODE=${PIPESTATUS[0]}
          echo "exit_code=${EXIT_CODE}" >> $GITHUB_OUTPUT

          if [ $EXIT_CODE -eq 0 ]; then
            echo "✅ Weekly update completed successfully"
          else
            echo "❌ Weekly update failed with exit code ${EXIT_CODE}"
            tail -100 ../weekly_update.log
          fi

      - name: Save updated context to parquet
        if: steps.update.outputs.exit_code == '0' && github.event.inputs.dry_run != 'true'
        run: |
          echo "========================================"
          echo "Saving updated context to parquet"
          echo "========================================"
          python - <<'PYEOF'
          import json
          import pandas as pd
          from pathlib import Path

          # Read the (potentially updated) league_context.json
          context_file = Path("league_context.json")
          if not context_file.exists():
              print("⚠️ league_context.json not found")
              exit(0)

          with open(context_file) as f:
              context = json.load(f)

          print(f"Saving context for: {context.get('league_name')}")
          print(f"  Years: {context.get('start_year')} - {context.get('end_year')}")
          print(f"  League IDs: {len(context.get('league_ids', {}))} years mapped")

          # Convert to a single-row DataFrame with JSON columns for complex fields
          row = {
              'league_id': context.get('league_id'),
              'league_name': context.get('league_name'),
              'game_code': context.get('game_code', 'nfl'),
              'start_year': context.get('start_year'),
              'end_year': context.get('end_year'),
              'num_teams': context.get('num_teams'),
              'playoff_teams': context.get('playoff_teams'),
              'regular_season_weeks': context.get('regular_season_weeks'),
              # Store complex objects as JSON strings
              'league_ids_json': json.dumps(context.get('league_ids', {})),
              'keeper_rules_json': json.dumps(context.get('keeper_rules')) if context.get('keeper_rules') else None,
              'manager_name_overrides_json': json.dumps(context.get('manager_name_overrides', {})),
              # Metadata
              'created_at': context.get('created_at'),
              'updated_at': context.get('updated_at'),
          }

          df = pd.DataFrame([row])

          # Save to fantasy_football_data so it gets uploaded with other parquets
          output_path = Path("fantasy_football_data/league_context.parquet")
          df.to_parquet(output_path, index=False)

          print(f"✅ Saved {output_path}")
          PYEOF

      - name: Upload updated data to MotherDuck
        if: steps.update.outputs.exit_code == '0' && github.event.inputs.dry_run != 'true'
        env:
          MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
          DATABASE_NAME: ${{ matrix.database }}
        run: |
          echo "========================================"
          echo "Uploading updated data to MotherDuck"
          echo "========================================"

          cd fantasy_football_data
          python motherduck_upload.py "${DATABASE_NAME}" . 2>&1 | tee ../motherduck_upload.log

          if [ $? -eq 0 ]; then
            echo "✅ Upload complete!"
          else
            echo "❌ Upload failed"
            tail -50 ../motherduck_upload.log
          fi

      - name: Re-aggregate matchup tables
        if: steps.update.outputs.exit_code == '0' && github.event.inputs.dry_run != 'true'
        env:
          MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
        run: |
          echo "========================================"
          echo "Re-aggregating matchup season/career/H2H tables"
          echo "========================================"

          source .venv/bin/activate
          python fantasy_football_data_scripts/multi_league/transformations/aggregation/aggregate_matchup_context.py \
            --context league_context.json \
            2>&1 | tee matchup_aggregation.log

          if [ $? -eq 0 ]; then
            echo "✅ Matchup aggregation complete!"
          else
            echo "❌ Matchup aggregation failed (non-critical)"
            tail -30 matchup_aggregation.log
          fi

      - name: Compute homepage summary
        if: steps.update.outputs.exit_code == '0' && github.event.inputs.dry_run != 'true'
        continue-on-error: true
        env:
          MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
        run: |
          echo "========================================"
          echo "Computing precomputed homepage summary"
          echo "========================================"

          source .venv/bin/activate
          python fantasy_football_data_scripts/multi_league/transformations/aggregation/homepage_summary.py \
            --context league_context.json --upload \
            2>&1 | tee homepage_summary.log

          EXIT_CODE=${PIPESTATUS[0]}
          if [ $EXIT_CODE -eq 0 ]; then
            echo "✅ Homepage summary complete!"
          elif [ $EXIT_CODE -eq 139 ]; then
            echo "⚠️ DuckDB segfault on shutdown (exit 139) — data already uploaded, ignoring"
          else
            echo "❌ Homepage summary failed"
            tail -30 homepage_summary.log
          fi

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: weekly-update-${{ matrix.database }}
          path: |
            weekly_update.log
            motherduck_upload.log
            matchup_aggregation.log
            homepage_summary.log
            league_context.json
          retention-days: 7

      - name: Update workflow summary
        if: always()
        run: |
          echo "# Weekly Update: ${{ matrix.database }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ steps.update.outputs.exit_code }}" == "0" ]; then
            echo "## ✅ Update Successful" >> $GITHUB_STEP_SUMMARY
          else
            echo "## ❌ Update Failed" >> $GITHUB_STEP_SUMMARY
          fi

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Database**: \`${{ matrix.database }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Dry Run**: ${{ github.event.inputs.dry_run || 'false' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Week**: ${{ needs.discover-leagues.outputs.current_week }}" >> $GITHUB_STEP_SUMMARY

  # Summary job
  summary:
    needs: [discover-leagues, update-league]
    if: always()
    runs-on: ubuntu-latest

    steps:
      - name: Create summary
        run: |
          echo "# Weekly Update Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Trigger**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Time**: $(date -Iseconds)" >> $GITHUB_STEP_SUMMARY
          echo "- **NFL Week**: ${{ needs.discover-leagues.outputs.current_week }} (detected once for all leagues)" >> $GITHUB_STEP_SUMMARY
          echo "- **Leagues Processed**: ${{ needs.discover-leagues.outputs.league_count }}" >> $GITHUB_STEP_SUMMARY
